const Title = ({title}) => {
    return (
        <h1 className="text-[28px] transition-all hover:translate-x-1 w-fit font-[700] max-w-[600px] text-black capitalize">{title}</h1>
    )
}

export default Title